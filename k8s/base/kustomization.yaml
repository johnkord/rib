apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: rib
resources:
  - namespace.yaml
  - configmap.yaml
  - postgres-statefulset.yaml
  - redis-deployment.yaml
  - minio-statefulset.yaml
  - backend-deployment.yaml
  - ingress.yaml
# Secrets are intentionally excluded; create out-of-band or via overlays
