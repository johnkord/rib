# Example environment variables for rib

# Required: JWT secret for authentication (minimum 32 characters)
JWT_SECRET=change-me-super-secret-jwt-key-at-least-32-characters-long

# Log level
RUST_LOG=info

# Database configuration
# For local development (docker-compose services):
DATABASE_URL=postgres://postgres:postgres@localhost:5432/rib
# For containerized setup, this is overridden in docker-compose.yml

# Redis configuration  
# For local development:
REDIS_URL=redis://localhost:6379
# For containerized setup, this is overridden in docker-compose.yml

# S3/MinIO configuration
# For local development:
S3_ENDPOINT=http://localhost:9000
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_BUCKET=rib-images

# Frontend configuration
FRONTEND_URL=http://localhost:5173

# Data directory for in-memory store snapshots
RIB_DATA_DIR=./data

# Optional Discord OAuth (for authentication)
# DISCORD_CLIENT_ID=your-discord-app-id
# DISCORD_CLIENT_SECRET=your-discord-secret
# DISCORD_REDIRECT_URI=http://localhost:8080/api/v1/auth/discord/callback

# Bootstrap admin users by Discord ID (comma-separated)
# BOOTSTRAP_ADMIN_DISCORD_IDS=188880431955968000
